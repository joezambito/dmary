//
//  MaryClarificationGuard.swift
//  Mary
//
//  Created by Joe Zambito on 27/4/2026.
//

import Foundation

struct MaryClarificationGuard {
}

/*

PASTED OLD CODE KEPT FOR LATER TESTING

static func analyzeComplexity(for message: String) -> MaryWorkMode {
    return MaryBrain.determineDepth(for: message)
}

static func getContextInstructions(for mode: MaryWorkMode) -> String {
    let base = "You are Mary, an expert Swift/macOS engineer."

    switch mode {
    case .basic:
        return "\(base) Mode: Basic. Focus on speed and direct syntax fixes. No long explanations."
    case .normal:
        return "\(base) Mode: Normal. Provide functional code with brief architectural context."
    case .complex:
        return "\(base) Mode: Complex. Perform a full system-wide analysis. Check for memory leaks, M2 efficiency, and thread safety."
    }
}

*/
